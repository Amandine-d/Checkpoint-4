const User = require("./user.models");
const Project = require("./project.models");
const Image = require("./image.models");
const Testimonial = require("./testimonial.models");

module.exports = { User, Project, Image, Testimonial };
